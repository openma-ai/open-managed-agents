/** @deprecated Use `SessionThreadStore`. */
export type {
  ArchiveSessionThread as ArchivePersistedSessionThread,
  ArchiveSessionThreadResult as ArchivePersistedSessionThreadResult,
  ListSessionThreads as ListPersistedSessionThreads,
  SessionThreadListPosition,
  SessionThreadLocation,
  SessionThreadStore as SessionThreadPersistencePort,
} from "@open-managed-agents/session-thread-store";
