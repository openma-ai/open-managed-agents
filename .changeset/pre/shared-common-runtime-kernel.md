---
"@openma/cli": patch
---

Consume the versioned OpenMA common local runtime for ACP sessions, connection lifecycle and owned-host shutdown. Keep CLI credentials, signals and filesystem adapters outside the shared kernel.
