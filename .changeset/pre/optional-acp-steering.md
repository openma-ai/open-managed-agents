---
"@openma/cli": patch
---

Update the shared runtime so ACP sessions can start without the optional steering extension. Reject unsupported active-turn steering at invocation instead of blocking ordinary prompts.
